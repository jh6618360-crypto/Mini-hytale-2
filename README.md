<!DOCTYPE html><html lang="pt-BR"><head><meta charset="UTF-8"><meta name="viewport" content="width=device-width,initial-scale=1,maximum-scale=1,user-scalable=no"><title>Mini Hytale Auto</title><style>*{margin:0;padding:0;box-sizing:border-box}body{background:#000;overflow:hidden;touch-action:none;font-family:Arial}canvas{display:block}#ui{position:fixed;inset:0;pointer-events:none;color:#fff}#crosshair{position:fixed;top:50%;left:50%;font-size:35px;transform:translate(-50%,-50%)}#hotbar{position:fixed;bottom:15px;left:50%;transform:translateX(-50%);display:flex;gap:8px;background:rgba(0,0,0,0.7);padding:10px;border-radius:12px;pointer-events:all}.slot{width:60px;height:60px;border:4px solid #555;border-radius:10px;background-size:cover;display:flex;align-items:flex-end;justify-content:flex-end;color:#fff;font-weight:bold;font-size:22px;padding:5px}.slot.selected{border-color:#fff;transform:scale(1.2)}#joyDiv{position:fixed;left:20px;bottom:80px;width:130px;height:130px;background:rgba(255,255,255,0.1);border-radius:50%}#jumpBtn{position:fixed;right:20px;bottom:80px;width:90px;height:90px;background:#0f08;border-radius:50%;display:flex;align-items:center;justify-content:center;font-size:50px;pointer-events:all}</style></head><body><div id="ui"><div id="crosshair">+</div><div id="hotbar"><div class="slot selected" data-block="grass" style="background-image:url(https://i.imgur.com/3z5bBZd.png)">1</div><div class="slot" data-block="dirt" style="background-image:url(https://i.imgur.com/5p3TkmG.png)">2</div><div class="slot" data-block="stone" style="background-image:url(https://i.imgur.com/8Q5jX3P.png)">3</div><div class="slot" data-block="wood" style="background-image:url(https://i.imgur.com/0Kx2y0Q.png)">4</div><div class="slot" data-block="sand" style="background-image:url(https://i.imgur.com/X5y5g5Q.png)">5</div></div><div id="joyDiv"></div><div id="jumpBtn">JUMP</div></div><script type="module">import*as THREE from'https://cdn.jsdelivr.net/npm/three@0.168/build/three.module.js';const scene=new THREE.Scene();scene.background=new THREE.Color(0x87ceeb);const camera=new THREE.PerspectiveCamera(75,innerWidth/innerHeight,0.1,1000);camera.position.set(0,10,10);const renderer=new THREE.WebGLRenderer({antialias:true});renderer.setSize(innerWidth,innerHeight);document.body.appendChild(renderer.domElement);scene.add(new THREE.AmbientLight(0xffffff,0.6));scene.add(((l=new THREE.DirectionalLight(0xffffff,0.8)).position.set(50,100,30),l));const tipos={grass:0x7ec850,t:0x8b5e3c,stone:0x888888,wood:0x8b5a2b,sand:0xf4d03f};let bloco='grass',map=new Map();function add(x,y,z,t=bloco){let k=`\( {x}, \){y},\( {z}`;if(map.has(k))return;let m=new THREE.Mesh(new THREE.BoxGeometry,new THREE.MeshLambertMaterial({color:tipos[t]}));m.position.set(x+.5,y+.5,z+.5);scene.add(m);map.set(k,m)}function rem(x,y,z){let k=` \){x},\( {y}, \){z}`;let m=map.get(k);if(m){scene.remove(m);map.delete(k)}}for(let x=-30;x<30;x++)for(let z=-30;z<30;z++){add(x,0,z,'grass');add(x,-1,z,'t');add(x,-2,z,'t')}for(let i=0;i<30;i++){let x=Math.floor(Math.random()*50)-25,z=Math.floor(Math.random()*50)-25,h=5+Math.floor(Math.random()*4);for(let y=1;y<=h;y++)add(x,y,z,'wood');for(let a=-2;a<=2;a++)for(let b=-2;b<=2;b++)if(Math.abs(a)+Math.abs(b)<4)add(x+a,h+1,z+b,'grass')}let mx=0,mz=0,vy=0,jump=true;const joy=nipplejs.create({zone:document.getElementById('joyDiv'),mode:'static',position:{left:'90px',bottom:'90px'},color:'white'});joy.on('move',(e,d)=>{mx=d.vector.x*0.35;mz=-d.vector.y*0.35});joy.on('end',()=>{mx=mz=0});document.getElementById('jumpBtn').ontouchstart=e=>e.preventDefault()||jump&&vy=0.35;let timer;renderer.domElement.ontouchstart=e=>{e.preventDefault();timer=setTimeout(()=>ray(1),450)};renderer.domElement.ontouchend=e=>{e.preventDefault();clearTimeout(timer);ray(0)};function ray(place){const rc=new THREE.Raycaster();rc.setFromCamera(new THREE.Vector2(0,0),camera);const hits=rc.intersectObjects(Array.from(map.values()));if(!hits.length)return;const p=hits[0].object.position.clone().sub(new THREE.Vector3(.5,.5,.5)).floor();place?add(p.x+hits[0].face.normal.x,p.y+hits[0].face.normal.y,p.z+hits[0].face.normal.z,bloco):rem(p.x,p.y,p.z)}document.querySelectorAll('.slot').forEach(s=>s.onclick=()=>{document.querySelector('.selected').classList.remove('selected');s.classList.add('selected');bloco=s.dataset.block});function loop(){requestAnimationFrame(loop);vy-=0.018;camera.position.x+=mx;camera.position.z+=mz;camera.position.y+=vy;if(camera.position.y<2.3){vy=0;camera.position.y=2.3;jump=true}renderer.render(scene,camera)}loop();addEventListener('resize',()=>{camera.aspect=innerWidth/innerHeight;camera.updateProjectionMatrix();renderer.setSize(innerWidth,innerHeight)})</script><script src="https://cdn.jsdelivr.net/npm/nipplejs@0.10.0/dist/nipplejs.min.js"></script></body></html>
